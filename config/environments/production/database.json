{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "bookshelf",
      "settings": {
        "client": "mysql",
        "database": "serve-me",
        "host": "${process.env.DB_URL}",
        "username": "root",
        "password": "housirvip"
      },
      "options": {}
    }
  }
}
